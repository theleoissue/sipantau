<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!--
            Latar jendela yang tampil selama WebView belum menggambar apa pun.
            Tanpa baris ini tema DayNight memakai latar bawaan sistem — hitam
            pada HP dengan mode gelap — dan itulah layar hitam sesaat saat APK
            dibuka. BridgeActivity menyetel tema ini sendiri di onCreate, jadi
            di sinilah tempatnya, bukan di tema peluncuran.
        -->
        <item name="android:windowBackground">@color/latar_sipantau</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
