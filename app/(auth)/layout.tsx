// Panggung navy penuh layar untuk halaman masuk dan ganti kata sandi,
// mengikuti #masuk pada sipantau-mockup-v2-sprin.html.

export default function TataLetakAuth({ children }: { children: React.ReactNode }) {
  return <div id="masuk">{children}</div>
}
